---
name: cognitive-load-optimizer
description: "Reduce developer cognitive load through 4 modes: shield (command risk labeling via PreToolUse hook), resume (session recovery assist with phase estimation), flow (progressive disclosure templates and smart defaults), density (cognitive density detection with Plan Mode auto-suggestion and decision scaffolding). Use when starting a new session, returning from compact, facing complex design decisions, or wanting to optimize information presentation. Invoke with /cognitive-load-optimizer."
user-invocable: true
---

# cognitive-load-optimizer

開発者の認知負荷を減らす4モード統合スキル。

## Modes

### `shield` — コマンドリスクラベリング

PreToolUse hook (`~/.claude/hooks/command-shield.sh`) が Bash コマンド実行前にリスクレベルを自動判定する。

| レベル | ラベル | 条件 |
|--------|--------|------|
| 🟢 safe | 読み取り専用・副作用なし | `ls`, `cat`, `git status`, `git log`, `echo`, `which` |
| 🟡 review | 変更あるが可逆 | `git add`, `git commit`, `npm install`, `mkdir` |
| 🔴 destructive | 不可逆・影響大 | `rm -rf`, `git push --force`, `DROP TABLE`, `git reset --hard` |

**常時有効**（hook ベース）。スキル呼び出し不要。

### `resume` — セッション復帰アシスト

Compact やセッション切れ後に「今どこにいるか」を要約して提示する。

**呼び出し**: `/cognitive-load-optimizer resume`

#### 処理手順

1. 以下のファイル/コマンドから情報収集:
   - `~/.claude/session-env/compact-state.md` — 最後の Compact 状態
   - `~/.claude/session-env/uncommitted-changes.md` — 未コミット変更
   - `~/.claude/session-env/last-skill.txt` — 最後に使ったスキル
   - `git status --short` — 現在の Git 状態
   - `git log --oneline -5` — 直近コミット

2. Git 状態からフェーズを推定:

| パターン | 推定フェーズ |
|---------|------------|
| 未追跡ファイルあり + コミットなし | 🔰 初期実装中 |
| ステージ済み変更あり | 📦 コミット準備中 |
| 直近コミットが `fix:` or `WIP` | 🔧 修正・デバッグ中 |
| ブランチが `feature/*` + コミット複数 | 🚀 機能開発進行中 |
| ブランチが `main` or `master` | 🏠 メインブランチ（タスク選択待ち） |

3. 以下のテンプレートで出力:

```
📍 セッション復帰サマリー
━━━━━━━━━━━━━━━━━━━━━
フェーズ: [推定フェーズ]
ブランチ: [ブランチ名]
未コミット: [ファイル数]ファイル変更
最後のスキル: [スキル名]

直近の作業:
- [コミットハッシュ] [メッセージ]
- ...

💡 推奨アクション: [状況に応じたアクション]
```

### `flow` — 段階的情報提示

Claude の出力パターンを認知負荷最適化する指針を適用する。

**呼び出し**: `/cognitive-load-optimizer flow`

#### 3つの原則

**1. プログレッシブ・ディスクロージャ**
- 情報を段階的に提示し、一度に読ませる量を制限
- 概要 → 詳細の順。最初から全部出さない

**2. スマートデフォルト**
- 頻出パターンにデフォルト値を提案し、判断回数を減らす
- ブランチ名 → `feature/[タスク要約]`
- コミットメッセージ → 変更内容の日本語要約
- テスト実行 → `vitest run [変更ファイルの隣]`
- エラーハンドリング → Result パターン

**3. 判断コスト削減テンプレート**
```
[推奨] Option A: 〜〜〜（理由: 最もシンプル）
[代替] Option B: 〜〜〜（理由: 拡張性重視）
→ 特に希望なければ A で進めるよ？
```

### `density` — 認知密度マネジメント

AI がルーチンワークを消すと、残る作業の認知密度が上がる（METR研究: シニア開発者がAI使用時19%遅くなった事例）。このモードは「難しい判断」のタイミングを検知し、思考の足場を作る。

**呼び出し**: `/cognitive-load-optimizer density`

#### 認知密度シグナル

以下のパターンを検知したら自動で Plan Mode を提案:

| シグナル | 検知方法 | 対応 |
|---------|---------|------|
| 3ファイル以上の同時変更 | git diff --name-only のカウント | Plan Mode で設計してから実装 |
| 設計判断を含む質問 | 「どっちがいい」「AとBどちら」等 | 選択肢を構造化して提示（flow テンプレート） |
| 3回連続の修正失敗 | tool-failures.jsonl の連続失敗 | 一度立ち止まって systematic-debugging へ誘導 |
| 新しいアーキテクチャパターン | 未使用の依存追加・新ディレクトリ作成 | brainstorming スキルで要件整理を提案 |
| 長時間セッション（90分超） | セッション開始からの経過時間 | /compact を提案 + 休憩リマインド |

#### 判断足場テンプレート

難しい設計判断に直面したとき、以下のフレームワークで思考を構造化:

```
🧠 判断ポイント検知

【何を決めるか】: [判断の要約]
【選択肢】:
  A: [選択肢A]（メリット: / デメリット: ）
  B: [選択肢B]（メリット: / デメリット: ）
【判断基準】: [最も重要な基準]
【可逆性】: 🟢 簡単に戻せる / 🟡 コストかかる / 🔴 不可逆
【推奨】: [推奨とその理由]
```

#### Plan Mode 自動提案ルール

- 影響ファイル3つ以上 → 「Plan Mode で設計してから進める？」
- 新しいパッケージ追加 → 「brainstorming で要件整理してから？」
- 連続失敗3回 → 「systematic-debugging に切り替える？」
- セッション90分超 → 「一度 /compact して整理する？」

## 呼び出し方法

```
/cognitive-load-optimizer           → 全モードの状態を表示
/cognitive-load-optimizer shield    → shield モードの状態確認
/cognitive-load-optimizer resume    → セッション復帰サマリーを表示
/cognitive-load-optimizer flow      → flow 原則をリマインド
/cognitive-load-optimizer density  → 認知密度チェック＋Plan Mode提案
```

## Cross-references

- Hook 実装: `~/.claude/hooks/command-shield.sh`
- Hook 登録: `~/.claude/settings.json` (PreToolUse > Bash)
- フェーズ表示: `~/.claude/hooks/statusline.sh`
- Flow 原則: `~/.claude/CLAUDE.md` (Output > 認知負荷最適化)
- 詳細リファレンス: `~/.claude/skills/cognitive-load-optimizer/reference.md`
- **skill-forge**: スキル設計における段階的情報開示（SKILL.md → reference.md の2層構造）の原則参照元
